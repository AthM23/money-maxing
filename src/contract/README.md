# src/contract — the one jointly owned directory

**Status: DRAFT by Person A, 2026-09-19 ~19:50 ET. Not frozen. Person B has not reviewed it.**
Per `context/ROADMAP.md`, this directory changes only with both people in the conversation.

| File | Source |
|---|---|
| `types.ts` | `Proposal`, `Mark` from spec §8; routes from `tests/README.md`; block rules from board sheet 04 |
| `tools.ts` | tool names from spec §8 |
| `topics.ts` | the 50 topics in `context/diagrams/architecture/EVENT_TOPICS.md` |
| `schema.sql` | spec §7 plus the additions the roadmap says to take before the freeze (each marked `-- ADDED`) |

Open points for the pairing session are marked `DECIDE:` in the files.
